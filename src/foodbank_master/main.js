console.log("Starting script...")

// select the table div

const table = document.getElementsByClassName('card')[0];
console.log(card);

function createCard(number) {
    return;
}
card.appendChild(createCard(1));
card.appendChild(createCard(2));